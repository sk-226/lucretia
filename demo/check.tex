% Lucretia theme check — check.tex (シンタックスには LaTeX Workshop 拡張が必要)
%
% チェック項目:
%   ① このコメントが斜体グレーで沈んでいる (Tier 4)
%   ② Cmd+F で「確認」を検索 → 現在の一致 = hl-yellow + 枠線、他の一致 = 半透明
%   ③ 括弧にカーソルを置く → 対応括弧に枠 (bracketMatch)
%   ④ \textbf 等の中身が実際に太字/斜体/下線で描画される
\documentclass{article}
\begin{document}

\section{見出しは太字になる}

{{{{{{}}}}}} // 連続括弧 6 段: blue→orange→magenta→cyan→purple→green で開き、逆順で閉じる

本文は tx のまま彩色されない。\textbf{太字}、\textit{斜体}、\emph{強調}、
\underline{下線}、\textbf{\textit{太字イタリック}} の入れ子を確認。

図~\ref{fig:model} と文献~\cite{lucretia2026} のキーは blue。ここでも確認。

数式 $E = mc^2$ と入れ子括弧 ${[({[(x)]})]}$ で括弧 6 色サイクルを確認。
閉じ忘れの括弧 ) は unexpectedBracket の赤になる。

\begin{quote}
  quote 環境の中身。環境名 quote の色も確認。
\end{quote}

\end{document}
