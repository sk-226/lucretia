# カラーテーマ設計計画 (plan.md)

Flexoki をベースに、背景をより白寄りに調整した自分用カラーテーマを設計する。
主用途はプレゼンテーション（ライト）、副用途は Markdown エディタ（ライト）、コーディング（ライト/ダーク）、そして**写真ギャラリー的な Web サイト**（写真が主役、UI・文字は控えめ）。
最終判断は主観で行うが、その判断を支える定量的な評価フレームワークを整備する。

---

## 1. コンセプト

- **紙の上のインク感は残す**が、Flexoki の paper (#FFFCF0) より**白寄りの背景**にする
- 背景とは**別に純白 (white) を独立した色として持ち**、背景の上に white を置いても区別できるようにする
- `colorname-数字` (50–950) のスケール体系（Flexoki 2.0 と同形式）
- **高コントラスト版を基本**とし、コンテンツ（写真・図など）を主役に立てる**静かな低コントラスト版**をサブとして用意する
- 全ての色の選定・調整を OKLCH 空間で行い、コントラスト・判別性を数値で管理する
- **尖らない・環境に依存しない**: 誰もが見慣れた慣習的な色使い（リンク=青、警告=赤 等）を基準に微調整する。Apple ディスプレイでも学会のプロジェクタでも大きく見え方が変わらないこと（sRGB 固定、劣化耐性 → §5.4）を設計条件とする
- **色だけに情報を載せない**: bg と white の差、微妙な色相差は「あると心地よい」装飾であって、意味の判別をそれに依存させない（劣化耐性・CVD 対応の共通原則）

---

## 2. Flexoki の調査結果

### 2.1 アーキテクチャ（公式サイトより）

- **構成**: モノクロの base スケール + アクセント 8 色 (red / orange / yellow / green / cyan / blue / purple / magenta)、各色 50–950 の 13 段階。`paper` (#FFFCF0) と `black` (#100F0F) は 0 / 1000 相当の特別値
- **ロール**: 各テーマは 8 つの base 値を使う — テキスト 3 (`tx`, `tx-2`, `tx-3`)、UI 3 (`ui`, `ui-2`, `ui-3`)、背景 2 (`bg`, `bg-2`)
- **アクセントの使い分け**: ライトテーマはシンタックス/テキストに **600**、ダークテーマは **400** を使用
- **導出原理**: Oklab 空間で知覚的な明度関係を維持しつつ、明・暗端に向かって**彩度 (chroma) を指数的に増加**させ、「薄めても鮮やかな水彩インク」の効果を再現。base 値は不透明度で導出できるが、**アクセントは不透明度導出だと彩度が失われるため不可**
- **設計思想の要点**: 知覚的均一性を厳密に守りすぎると「washed out で判読しにくいパレット」になるため、シンタックスハイライトでの識別性のために意図的に均一性を崩している

### 2.2 主要色の OKLCH 実測値（自分で計算・検証済み）

| 色 | Hex | L | C | H |
|---|---|---|---|---|
| paper | #FFFCF0 | 0.990 | **0.016** | 95° |
| base-50 | #F2F0E5 | 0.954 | 0.015 | 98° |
| base-600 | #6F6E69 | 0.538 | 0.008 | 97° |
| black | #100F0F | 0.170 | 0.002 | 17° |
| red-600 | #AF3029 | 0.504 | 0.165 | 28° |
| orange-600 | #BC5215 | 0.567 | 0.152 | 45° |
| yellow-600 | #AD8301 | 0.633 | 0.130 | 86° |
| green-600 | #66800B | 0.559 | 0.134 | 123° |
| cyan-600 | #24837B | 0.554 | 0.086 | 187° |
| blue-600 | #205EA6 | 0.482 | 0.132 | 255° |
| purple-600 | #5E409D | 0.454 | 0.145 | 295° |
| magenta-600 | #A02F6F | 0.495 | 0.161 | 350° |

観察:

1. **paper の C=0.016 は背景としてはかなり高彩度**（H≈95° = 黄〜オレンジ寄り）。これが「オレンジみがかかりすぎ」の定量的な正体。white (#FFFFFF, L=1.0, C=0) との Oklab 距離 ΔEok ≈ 0.019
2. 600 系アクセントの L は 0.48–0.63 と**意図的に不均一**（識別性優先）。特に yellow-600 は paper 上で WCAG 3.39:1 と本文用途には不足 → 自作テーマでも黄色は要注意色
3. base スケールは H≈97° でほぼ一定、C は 0.008–0.016。black だけ H=17°（わずかに赤み）

### 2.3 paper 上の WCAG コントラスト実測

| 前景 | 比 | 評価 |
|---|---|---|
| black | 18.6:1 | AAA |
| base-600 (tx-2) | 5.0:1 | AA |
| base-300 (tx-3) | 2.0:1 | 装飾のみ |
| blue-600 | 6.4:1 | AA+ |
| red-600 | 6.2:1 | AA+ |
| green-600 | 4.4:1 | AA 弱 |
| yellow-600 | 3.4:1 | 大文字のみ |

---

## 3. 要求仕様の整理

### 3.1 カラー名とスケール

- カラー名（10）: アクセント 8 色 `red` `orange` `yellow` `green` `cyan` `blue` `purple` `magenta` + `black` `white`（Flexoki と同じ 8 アクセント構成）
- `white` は Flexoki の paper と違い**真っ白 #FFFFFF（または極めて近い色）**を割り当て、背景 `bg` とは別物にする
- スケール: 各アクセント色に 50–950（小さい番号 = 明るい、Flexoki/Tailwind 方式を踏襲）
- ニュートラル: `black`/`white` を端点とするモノクロスケールが必要（名前案: `base`）。本文の「黒〜少しネイビーめ」をどう表現するかは §4.2

### 3.2 用途とテーマ

| 用途 | テーマ | 優先度 |
|---|---|---|
| プレゼンテーション | ライト | ◎ 最優先 |
| Markdown エディタ | ライト | ○ |
| コーディング | ダーク + ライト | ○（Flexoki の不満点を改善 → §6.3） |
| Web / 写真ギャラリーサイト | ライト（ダークは要検討） | ○（写真が主役、UI は静かに → §6.4） |

### 3.3 プレゼンでの色ロール（要件から）

- 背景: bg（白寄りの温白）
- 本文: 黒 or 少しネイビーめの濃色
- サブテキスト: 灰色
- 強調: **青 or 赤**
- サブカラー: 緑・オレンジ・黄（状況で使い分け）
- 薄色 (50–200 帯): **文字色としても背景（塗り）としても**使う → 両用途での基準を定義する（§5.3）

### 3.4 コントラスト 2 バージョン

- **高コントラスト版（基本）**: 本文 WCAG ≥ 7:1 / APCA |Lc| ≥ 90 目標。読み物・数式・プレゼン本文向け
- **低コントラスト版（サブ）= "quiet プロファイル"**: ソフトな見た目にしつつ、**テキスト・UI を後退させて写真や図などのコンテンツを主役に立たせる**ことが目的。想定用途はギャラリーサイト、写真・図が主体のスライド
  - 本文はフロアとして WCAG ≥ 4.5:1 / |Lc| ≥ 75 を維持（読める範囲で最も静かに）
  - UI 要素（罫線・アイコン等）≥ 3:1 / |Lc| ≥ 30、装飾は自由
  - 有彩色の使用量を絞る（アクセントはリンク/フォーカス等の最小限）
- 実装イメージ: ロールの割当だけ変える（例: 高 = tx に base-950 級、低 = tx に base-700 級 + アクセント使用箇所の削減）。パレット自体は共通

---

## 4. 設計方針

### 4.1 背景色 bg（本テーマの核心）

方針: **paper の色相 (H≈95°) は維持し、彩度 C を下げる**。L はわずかに調整可。

初期候補（OKLCH から逆算、sRGB 内、計算検証済み）:

| 案 | OKLCH | Hex | ΔEok vs white | ΔEok vs paper | 印象の仮説 |
|---|---|---|---|---|---|
| A（微温） | (0.990, 0.006, 95°) | #FDFCF7 | 0.012 | 0.009 | paper と white の中間 |
| B（やや温） | (0.990, 0.008, 95°) | #FDFCF6 | 0.013 | 0.008 | A よりわずかに紙感 |
| C（白寄り） | (0.993, 0.006, 95°) | #FEFDF8 | 0.009 | 0.010 | 最も白に近い |

- **white との判別基準（定量）**: bg 上に white のボックスを置いたとき並置で判別できること。目安として ΔEok ≥ 0.008–0.010（≈ 並置なら判別可能、単独では「白」と感じるレベル）を下限とし、実機で検証
- bg-2（セカンダリ背景）は bg より一段濃い base-50 級を再設計（同じ H、C は bg よりわずかに高くてよい）
- **検証方法**: 候補 3 案 × 複数ディスプレイ（sRGB / P3、輝度高低）× 明所/暗所で A/B 並置比較。white ボックス・本文・アクセントを載せた同一サンプルスライドで比較する

### 4.2 ニュートラル（base スケール）と「ネイビーめの本文」

- base スケールの H を Flexoki (≈97°, 暖色) のままにするか、**より中立（C を下げる）に寄せる**かを Phase 1 で比較する。bg を白寄りにした分、暖色 base だと浮く可能性があるうえ、ギャラリー用途では base の中立性が効く（§6.4）
- 「ネイビーめの本文」は**案 2 で決定（優先度低）**: base は中立に保ち、基本の本文は black / base の黒・灰を使う。必要な場面でのみ `blue-900/950` 級をオプションの第 2 テキスト色（tx-navy）として使えるようにしておく（Flexoki blue-900 #1A4F8C は L=0.427, paper 上 WCAG 8.0:1 で本文可の水準。自作スケールでも同水準を確保する）
- 高/低コントラスト版の tx 割当（初期案、要 APCA 検証）:

| ロール | 高コントラスト | 低コントラスト |
|---|---|---|
| tx（本文） | black 級 (L≈0.17) | base-800 級 (L≈0.40) |
| tx-2（サブ） | base-700 級 | base-600 級 |
| tx-3（faint） | base-500 級 | base-400 級 |

### 4.3 アクセントスケールの生成規則

- 各色について **H をスケール全体でほぼ固定**（インク感のため Flexoki 同様、端でわずかな H ドリフトは許容）
- **L をステップ番号に対応付ける共通カーブ**を定義（全色共通の L ターゲット。Flexoki 実測から初期値を作る: 例 50→0.95, 200→0.85, 400→0.62±, 600→0.53±, 800→0.35, 950→0.20）
- **C はステップに対するカーブ**（中央 300–500 で最大、端で減衰しつつ Flexoki 流に「薄くしても彩度が残る」よう通常のベジェより高めに保つ）を色ごとにチューニング
- ただし §2.2 の知見どおり、**識別性のために色ごとの L 微調整を許す**（完全均一化はしない）。特に yellow / cyan は L を上げないと色として死ぬので、代わりに「本文級テキストには使わない」規約で対処
- 全値 **sRGB ガモット内**に収める（プレゼン投影・PDF・他人の環境との互換性のため）。P3 拡張は将来のオプション

### 4.4 薄色帯 (50–200) の両用途設計

- **背景（塗り）として**: bg の上に置いたとき塗りと分かること → bg との ΔEok ≥ 0.02 目安 + 枠線なしで判別できるか実機確認
- **文字として**: 濃色背景（ダークテーマ / 濃色塗り）上の文字に使う → 対象背景との APCA |Lc| ≥ 75 を満たす番号を用途表に明記（例: 「red-100 は red-800 以深の上でのみ文字可」）

---

## 5. 定量評価フレームワーク

### 5.1 使用する指標

| 指標 | 役割 | 目標・使い方 |
|---|---|---|
| **WCAG 2.x コントラスト比** | 互換性・事実上の標準。現行の法的基準もこれ | 本文 ≥ 7:1（高）/ ≥ 4.5:1（低）、大文字・UI ≥ 3:1 |
| **APCA Lc** | 知覚ベースの読みやすさ。WCAG 2.x が過大評価しがちな**ダークモードで特に重要**。フォントサイズ・ウェイトも考慮できる | 本文 \|Lc\| ≥ 90（高）/ ≥ 75（低）、大きめ・太字 ≥ 60、サブテキスト ≥ 45、非テキスト ≥ 30。**極性（符号）に注意**: ダークモードは負値 |
| **OKLCH (L, C, H)** | 設計空間。L 固定で色相間の見かけの重さを揃える、C カーブで彩度管理 | 全色の設計パラメータとして記録 |
| **ΔEok**（Oklab 距離） | 判別性: bg vs white、隣接ステップ間、アクセント同士 | bg vs white ≥ 0.008、隣接ステップ ≥ 0.02 目安 |
| **CVD シミュレーション** | 色覚多様性（P 型 / D 型 / T 型）での判別性 | 強調 (red/blue)・サブ (green/orange/yellow) が色相のみに依存しない設計か確認 |

注意点（調査より）:

- APCA は WCAG 3 の候補アルゴリズムだが、**WCAG 3 自体はまだドラフト段階で正式採用は未確定**（ARC = APCA Readability Criterion として独立標準化が進行中）。したがって **WCAG 2.x をフロア（最低保証）、APCA を設計指針**として併用するのが現時点の妥当な運用
- WCAG 2.x は暗い色ペアのコントラストを過大評価する（4.5:1 でも暗色同士は読めないことがある）ため、**ダークテーマの判定は APCA を主**にする

### 5.2 コントラスト行列（成果物）

全ロール組合せの自動計算表を作る:

- ライト: {tx, tx-2, tx-3, 各アクセント 600, 各アクセント 400} × {bg, bg-2, white, 各アクセント 50–200}
- ダーク: {tx, tx-2, tx-3, 各アクセント 400, 300} × {bg-dark, bg-dark-2, 各アクセント 850–950}
- 各セルに WCAG 比と APCA Lc を併記し、目標未達セルを可視化（合否ヒートマップ）

### 5.3 プレゼン特有の検証

- **プロジェクタ問題**: 投影では輝度レンジが狭く淡色が飛ぶ。薄色帯 (50–150) の塗りが投影で消えないか、実機またはガンマ劣化シミュレーション（コントラスト低下を模擬）で確認
- 会場後方からの視認を想定し、サブテキスト（灰色）の下限を通常の Web 基準より厳しめに設定（tx-3 は装飾専用と割り切る）

### 5.4 環境ロバスト性（設計条件）

制作環境は Apple ディスプレイ（P3・高輝度・精度良）だが、本番は学会・大学の任意のプロジェクタ / 安価な液晶。**「どこでもデフォルトで使えて、人や環境で見え方が大きく変わらない」**を条件化する:

- **sRGB 固定**: 全色 sRGB 内で定義し、P3 拡張はしない。カラーマネジメント無視の環境でも設計意図が保たれる
- **劣化モードを想定した設計**: 想定される劣化は (a) 淡色が白に飛ぶ、(b) 暗部が潰れる、(c) 色温度・彩度がずれる。これに対して:
  - bg の微彩度は「白に飛んでも一切困らない」位置づけにする（bg vs white の区別に意味を持たせない。§1 の原則）
  - 意味の判別は L 差（明度差）を主軸に設計し、コントラスト行列で保証する
  - 淡色塗り (50–150) は投影時に消えうる前提で、枠線 or 十分な L 差を併用するガイドラインを作る
- **検証**: Apple ディスプレイに加えて、非 Apple の安価なモニタ 1 台 + 実プロジェクタ 1 回以上で候補比較。簡易には「彩度 -20% / ガンマ変更 / 輝度低下」を模したシミュレーション画像でスクリーニング
- **慣習に沿う**: hue の割当は広く共有された慣習（リンク=青、エラー/警告=赤・橙、成功=緑、ANSI 16 色の並び）から外さない。見慣れた配色が一番読みやすい、を明示的な設計原則とする

---

## 6. 用途別マッピング（Flexoki の Mappings 表に相当するものを作る）

### 6.1 プレゼンテーション（ライト）

| ロール | 割当（初期案） |
|---|---|
| 背景 | bg / セカンダリ bg-2 / カード white |
| 本文 | tx（黒 or 微ネイビー） |
| サブテキスト | tx-2（灰） |
| 強調 1 / 2 | blue-600 / red-600 |
| サブカラー | green-600, orange-600, yellow-600（黄は太字・大サイズ限定） |
| 淡色塗り | 各色 100–150（例: red-100 の注意ボックス） |
| 淡色上の文字 | 同系統 700–800（例: red-100 上に red-800） |
| 図形・グラフ | chart-1〜7（MATLAB 同様 7 色。L 段差 300–600 帯混在で CVD 耐性を確保 → §9-12） |

### 6.2 Markdown エディタ（ライト）

見出し / 本文 / リンク / インラインコード / コードブロック背景 / 引用 / 太字強調 のロール表を作成。リンク = blue-600、コードブロック背景 = bg-2 or base-50 級、引用 = tx-2 + 左ボーダー ui 級、を初期案とする。

**ハイライター (hl-\*)**: Obsidian 等のマーカー線用に全 8 hue の専用色を持つ（L=0.93, C=0.85×Cmax。tint 150 より高彩度・高明度の「蛍光マーカー」の見え）。**規約: base の文字（tx / tx 太字）の下にのみ引く**。有彩色文字・リンクの上には使わない。黒文字で全色 WCAG ≥ 15:1 / |Lc| ≥ 90 を確認済み（→ §9-13）。

### 6.3 コーディング（ダーク + ライト）

**Flexoki への不満の診断**: 全トークンが彩度のある色で塗られていて視覚的な階層がなく、「どこを見ればいいか分からない」。コントラスト自体は足りていても、**注目誘導ができていない**のが問題。

**設計原則 — 輝度・彩度による階層化（sparse highlighting）**: 「何を目立たせるか」を先に決め、色は階層の道具として使う。

| 階層 | 対象 | 色の方針 |
|---|---|---|
| Tier 1（主役・最頻出） | 変数名・オブジェクト・識別子 | **tx そのもの**（ダーク: 白系 / ライト: 黒系）。彩色しない |
| Tier 2（構造の目印） | 関数・メソッド定義、キーワード | 2–3 hue に絞った中彩度色（例: 定義=blue, キーワード=magenta or purple） |
| Tier 3（リテラル） | 文字列、数値、定数 | Tier 2 と系統の違う hue（例: 文字列=green or cyan, 数値=purple） |
| Tier 4（背景化） | 演算子・句読点 = tx-2、コメント = tx-3 | 無彩色で後退させる |

- **1 画面に同時に出る有彩色を 4–5 hue 以下**に抑える（Flexoki は 6+ hue が常時出る）
- ダークテーマの tx（= 変数の色）は真っ白でなく **white〜base-100 級**から選定し、bg-dark との APCA で |Lc| ≥ 90 を確認
- ダークテーマの bg は black〜base-950 級から出発し、**APCA 基準で** tx / アクセント 400 帯を検証
- purple 採用により Flexoki の numbers=purple はそのまま流用可能。ただし上の階層原則を優先し、割当は Tier 設計から再構成する
- 参考テーマ（同思想）: Alabaster（「ハイライトは目立たせるためにある」思想の代表例）、GitHub テーマ系の控えめな配色
- 配布形式の初期ターゲット: VS Code テーマ + ターミナル (ANSI 16 色マッピング)。ANSI では red/green/yellow/blue/magenta/cyan + black/white が要求されるため、**今回の色名は ANSI と自然に整合する**（orange / purple は 256 色 or テーマ側拡張）

### 6.4 Web / 写真ギャラリーサイト

**方針**: 写真が主役。UI・タイトル・本文はニュートラル主体で静かに（quiet プロファイルの主用途）。

- **写真面の中立性（重要な技術論点）**: 有彩色の背景は色順応・同時対比により**写真のホワイトバランスの知覚を歪める**。bg（微温白, C≈0.007）をページ全体の地に使うのは可だが、**写真を直接載せる面・写真の直近**には C≈0 のニュートラル面（`white` / 無彩グレー / near-black）を使う「photo surface」ロールを別途定義する。bg 上に写真を直置きした場合の見え方は Phase 4 で実写画像を使って検証
- **アクセントの使用は最小限**: リンク・フォーカスリング・アクティブ状態に 1 hue（blue を既定）。それ以外は base で構成
- **ダークギャラリー**: 写真展示は暗背景も定番なので、C≈0 の near-black を photo surface のダーク版として用意（コーディング用ダーク bg と共有できるか検討）

**Extended palette（このために整備するロール群）**:

| ロール | 内容 |
|---|---|
| photo-surface / photo-surface-dark | 写真隣接面（C≈0） |
| link / link-hover / visited | blue 系 2–3 段 |
| border / hairline | ui 級 1–2 段（罫線・区切り） |
| overlay-scrim | black / white の**アルファ階段**（例: 5/10/20/40/60/80%）— 写真上のキャプション・ボタンの下敷き |
| caption / meta | tx-2 / tx-3 級（撮影情報・キャプション） |
| focus-ring | 高視認 1 色（アクセシビリティ用、blue-400/600 級） |

- scrim 上の文字（写真の上に白文字 + 黒スクリム等）は背景が任意の写真なので、**スクリム込みの実効コントラスト**で検証する（代表的な明・暗・カラフル写真のセットでワーストケースを測る）

---

## 7. 実装・ツール計画

| 目的 | ツール |
|---|---|
| 色空間変換・パレット生成 | Python: `coloraide`（OKLCH, ΔE, ガモットマッピング対応）。スケール生成スクリプトを自作 |
| APCA 計算 | `apca-w3` (npm) または Python 実装を自作（±の極性まで再現し、公式値との一致テストを書く） |
| 対話的チューニング | oklch.com 系ピッカー、Harmonizer / apcach（APCA 一定のままパレット生成）、atmos.style |
| コントラスト確認（手動） | contrast.tools（WCAG + APCA 併記） |
| CVD シミュレーション | `colorspacious` / Coblis 相当のシミュレーション画像を自動生成 |
| スウォッチ・サンプル | HTML 1 枚のスウォッチシート + サンプル（スライド 1 枚 / Markdown プレビュー / コード light & dark / **ギャラリーページのモック（実写画像入り）**）を自動生成し、候補比較を並置できるようにする |

**単一ソース**: `palette.json`（OKLCH パラメータ + hex + ロールマッピング）を正とし、CSS variables / VS Code theme / プレゼン用テンプレの色定義を全てここから生成する。

---

## 8. マイルストーン

- **Phase 0 — 環境整備**: 変換・APCA・ΔE の計算スクリプトと検証テスト（公式値と一致確認）
- **Phase 1 — bg と base の確定**: 背景候補 A/B/C + base の色相 2 案（Flexoki 風の微暖色 / より中立）を実機比較（Apple + 非 Apple モニタ） → bg, bg-2, white, photo-surface, base スケール確定
- **Phase 2 — アクセント生成**: L/C カーブ定義 → 8 色 × 13 段階を生成 → コントラスト行列 + CVD チェック → 目標未達を個別調整
- **Phase 3 — ロールマッピング**: 高コントラスト / quiet の 2 プロファイル、用途 4 種（プレゼン / Markdown / コード / ギャラリー）のマッピング表 + Extended palette を確定
- **Phase 4 — 実地検証**: サンプルスライドで実プレゼン（プロジェクタ 1 回以上）、Markdown 実運用、VS Code テーマ試作、実写画像でのギャラリーモック確認を 1–2 週間 → フィードバック反映
- **Phase 5 — パッケージ化**: palette.json / CSS / VS Code / ドキュメント（Flexoki サイト風の仕様ページ）

---

## 9. 決定事項と残る未決事項

### 決定済み

1. **purple 採用** → アクセント 8 色（Flexoki と同構成）
2. **ネイビー本文は案 2**（base の黒・灰が基本、blue-900 級 tx-navy はオプション。優先度低）
3. **コーディングテーマの改善方針** = 輝度・彩度による階層化。変数・オブジェクトは白系（tx）、彩色は構造の目印に絞る（§6.3）
4. **低コントラスト版** = quiet プロファイル。ソフトな見た目 + コンテンツ（写真・図）を主役に立てる（§3.4）
5. **環境方針** = Apple ディスプレイ制作・任意環境本番。sRGB 固定、劣化耐性、慣習に沿った尖らない配色（§5.4）
6. **用途に Web / 写真ギャラリーを追加**。Extended palette（photo-surface, scrim, link 等）を整備（§6.4）
7. **bg = 旧案 A**（OKLCH (0.990, 0.006, 95°) = #FDFCF7、bg-2 #F5F3EE）。実機比較で決定（2026-07, Phase 1 完了）
8. **base = neutral**（C≈0.002。Flexoki 風微暖色 warm 案は廃止）。ギャラリーの写真中立性を優先（2026-07, Phase 1 完了）
9. **Phase 2 検証結果と規約**（2026-07）:
   - yellow-600 は本文禁止・**太字/大サイズ限定**を規約化（bg 上 3.4:1 / Lc 60）
   - **orange と green をグラフ・図で隣接させない**（D型 CVD で ΔEok 0.018 とほぼ区別不能。out/cvd.html）。判別は L 差・形状を併用（§1 の原則）
   - 淡色帯 50–200 は「塗り」（全色 50 でも ΔEok ≥ 0.030 vs bg）「文字」（X-100 は同系統 800 以深上で |Lc| ≥ 78）とも基準クリア
10. **ダーク bg = black (#100F0E)**: base-950 だと tx=base-100 が Lc −88.5 で本文目標 90 に未達のため（APCA 実測）。bg-dark-2 = base-950
11. **シンタックス割当**（§6.3 の Tier 原則から確定）: 定義=blue / **キーワード=magenta**（purple は blue と D型 CVD で近すぎるため不採用）/ 文字列=green / **数値=purple** / 演算子=tx-2 / コメント=tx-3。ライトは 600 帯、**ダークは 300 帯中心**（400 帯は black 上で Lc −31〜−49 と暗すぎる。string のみ green-400）
12. **chart 7 色**（2026-07, MATLAB の 7 色構成に倣う）: blue-400 → orange-600 → purple-600 → red-300 → yellow-500 → cyan-600 → green-500。L 段差（300–600 帯混在）で CVD 耐性を確保し、貪欲法で「先頭 k 色の P/D/T 込み最悪ペア ΔEok」を最大化した優先順。全ペア最悪 0.042（旧 400 帯 5 色案は 0.018）。形状・線種の併用規約（§9-9）は継続
13. **ハイライター hl-\***（2026-07）: 全 8 hue、OKLCH (0.93, 0.85×Cmax, H)。黒文字（tx / tx 太字）専用の規約。全色 ≥ 15:1 / |Lc| ≥ 90（L=0.93 は基準を満たす下限）
14. **photo-surface ライト = bg**（2026-07）: 真っ白は使わない。**white は「際立たせる」用途（カード・強調面）専用**という白の使い分け原則による。§6.4 の「写真直近は C≈0」の懸念（bg は C=0.006）は Phase 4 の実写検証で確認する
15. **photo-surface ダーク = base-950**（2026-07, 実写モックで確認して決定）: ダークギャラリーは採用。コーディング用ダーク bg (black) とは別で、黒より一段浮かせた C≈0 面を写真の地にする

### 未決（Phase 4 以降で判断）

1. **エディタ対応範囲**: VS Code + ターミナル以外（JetBrains, Neovim 等）をいつやるか

---

## 参考

- Flexoki 公式: https://stephango.com/flexoki / リポジトリ: https://github.com/kepano/flexoki
- APCA: APCA in a Nutshell (git.apcacontrast.com) / SAPC-APCA リポジトリ (github.com/Myndex/SAPC-APCA) / W3C Silver Visual Contrast wiki
- WCAG 3 の現状: 2023-06 の Editor's Draft でコントラスト基準は placeholder に戻され、APCA は ARC として独立発展中
- OKLCH: oklch.fyi / Evil Martians の OKLCH エコシステム記事（Harmonizer, apcach, Polychrom）
- Oklab: Björn Ottosson, "A perceptual color space for image processing"
